# Serverless-backend-For-a-TODOs-App-using-lambda
A serverless backend for a TODO application built using AWS Lambda, enabling scalable execution of CRUD operations without managing servers. It integrates with Amazon API Gateway for routing requests and uses cloud storage (like Amazon DynamoDB) to store and manage tasks efficiently.
