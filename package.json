{
  "name": "todos-serverless-backend",
  "version": "1.0.0",
  "description": "Serverless TODOs backend — API Gateway + Lambda + Lambda Authorizer + DynamoDB",
  "scripts": {
    "dev": "serverless offline --httpPort 4000",
    "deploy": "serverless deploy",
    "deploy:prod": "serverless deploy --stage prod",
    "remove": "serverless remove"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.484.0",
    "@aws-sdk/lib-dynamodb": "^3.484.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "serverless-offline": "^13.3.0"
  }
}
